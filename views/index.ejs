<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>Webshop</title>
		<link href="/css/style.css" rel="stylesheet" type="text/css">
		<meta name="viewport" content="width=device-width, initial-scale=1">
	</head>
	<body>
		<header>
			<h2>Webshop</h2>
		</header>
		<nav>
			<a href="/">Termékek</a>
			<a href="/register">Regisztráció</a>
			<a href="/login">Bejelentkezés</a>
			<!-- ha user akkor csak logout + admin linkje -->
		</nav>
		<section>
			<aside>
				<form action="/search">
					<input type="text" name="search">
					<button type="submit">Keresés</button>
				</form>
				<hr>
				<ul>
					<!-- MINTA: csak ezért van itt hogy meg lehessen nézni az oldalt. -->
					<% product_types = [
						{"name":"asd"},
						{"name":"xd"},
						{"name":"mukodik"},
						{"name":"a fasze vagyok huulye"}
					] %>
					<% for (const pt of product_types) { %>
						<li><a href="filter/<%= pt.name%>"><%= pt.name%></a></li>
					<% } %>
				</ul>
			</aside>
			<article>
				<!-- MINTA: csak ezért van itt hogy meg lehessen nézni az oldalt. -->
				
				<% for (const termek of termekek) { %>
					<div onclick="location.href='/product/<%= termek.nev%>'">
						<image src="images/<%= termek.kep%>" alt="<%= termek.nev%>" width="180" height="180"></image>
						<p class="name"><%= termek.nev%></p>
						<p class="price"><%= termek.ar%> Ft</p>
					</div>
				<% } %>
			</article>
		</section>
		<footer></footer>
  	</body>
</html>